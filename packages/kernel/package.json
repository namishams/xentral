{
  "name": "@xentral/kernel",
  "version": "0.1.0",
  "description": "Xentral frozen core — tenancy, identity, auth, party, permissions, document backbone. Change-controlled (owner approval).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "build": "tsc --noEmit", "typecheck": "tsc --noEmit", "lint": "echo ok", "test": "vitest run" }
}
