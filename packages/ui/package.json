{
  "name": "@xentral/ui",
  "version": "0.1.0",
  "description": "Xentral locked component library (the only UI a page may import).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "dependencies": { "@xentral/config": "workspace:*" },
  "peerDependencies": { "react": "^18 || ^19", "react-dom": "^18 || ^19" },
  "devDependencies": { "@types/react": "^18", "@types/react-dom": "^18" },
  "scripts": { "build": "tsc --noEmit", "typecheck": "tsc --noEmit", "lint": "echo ok" }
}
