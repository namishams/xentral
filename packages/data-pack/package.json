{
  "name": "@xentral/data-pack",
  "version": "0.1.0",
  "description": "Live data source adapter (driver-injected) implementing the kernel DataSource port against the existing app schema.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "dependencies": { "@xentral/kernel": "workspace:*" },
  "scripts": { "build": "tsc --noEmit", "typecheck": "tsc --noEmit", "lint": "echo ok", "test": "vitest run" }
}
