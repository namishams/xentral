{
  "name": "@xentral/update-pack",
  "version": "0.1.0",
  "description": "Default swappable update strategy (Changesets). Implements the kernel UpdatePort. Decoupled per package.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "dependencies": { "@xentral/kernel": "workspace:*" },
  "scripts": { "build": "tsc --noEmit", "typecheck": "tsc --noEmit", "lint": "echo ok", "test": "vitest run" }
}
