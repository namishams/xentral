# Xentral (modular monorepo)\n\nProven stack: pnpm workspaces + Turborepo + Changesets.\nSee ARCHITECTURE.md for the full blueprint and export plan.\n\n- packages/kernel  — frozen core (owner-approval to change)\n- packages/ui      — locked component library\n- packages/config  — design tokens / ui-constants / layout / component registry\n- modules/*        — business modules (contract-bounded)\n- plugins/*        — installable plugins\n- apps/web         — composed Next.js app\n