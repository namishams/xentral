---
"@xentral/kernel": minor
"@xentral/data-pack": minor
"@xentral/module-marketplace": minor
"@xentral/web": minor
---

Mediflow lead-sale connector — read side. DataPort gains RawMarketLead + listMarketplaceLeads; the data-pack adapter reads real marketplace_leads (status AVAILABLE, not sold out) from the existing schema. module-marketplace gains loadMarketLeads(scope): live listings mapped to the masked card shape with the dutch-auction current price, next-drop countdown and channel flags; seed fallback on preview. The marketplace page is split into a server loader + client grid. Dormant on the public preview (no scope/source → seed); live behind auth at go-live. Read-only — the buy/credit flow is the next step.
