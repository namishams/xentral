---
"@xentral/web": minor
---

Mediflow lead-sale — buy/write side + e-mail function (both dormant). New /api/marketplace/[id]/buy replays the live purchase as a serializable SQL transaction: availability re-check, double-purchase + suspension + daily-limit guards, credit verification, atomic credit decrement, lead update (mark SOLD when exclusive/maxed), purchase row + credit_transactions ledger entry; best-effort CRM contact creation and a purchase-confirmation email. New apps/web/lib/email.ts (nodemailer) ports the e-mail function with sendMail + sendPurchaseConfirmation. Both are DORMANT: buy returns 503 unless XENTRAL_LIVE_DATA=1 + DATABASE_URL + a session; email is a no-op until SMTP_* env is set. Public preview unaffected (503 / no-op / seed).
