---
"@xentral/module-crm": minor
---

Universal Timeline (Roadmap Phase 1): module-crm loadTimeline(scope) composes
loadActivities + loadTasks into a merged, reverse-chronological feed through the same
DataPort — no new adapter. /timeline is now a session-scoped server component.
