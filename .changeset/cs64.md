---
"@xentral/web": minor
"@xentral/ui": minor
---

Branding + auth parity: real Xentral mark (XentralMark, twin gradient waves) in @xentral/ui, used in the app sidebar/header; real favicon/icon set + layout icon metadata copied from the existing app. The /auth/login page is now a faithful port of the live login (mesh background, Welcome to Xentral, password + email-code toggle, Create an account link) wired to the SessionPort seam. First step of the "new build becomes app.xentral.ae" migration.
