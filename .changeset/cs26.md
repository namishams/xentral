---
"@xentral/web": minor
---

Add Deal record/detail page (/deals/[id]) — action bar, KPI cards, activity timeline, summary + linked records + attachments rail. First record-layer template. Deals list rows now link to it.
