---
"@xentral/config": minor
"@xentral/ui": minor
"@xentral/web": minor
---

Global theming: token layer (@xentral/config color) now resolves to CSS variables; light + dark (SAP Fiori Horizon Night) defined globally with a pre-paint, no-flash theme script. Locked ThemeToggle in the header flips document[data-theme] and persists per user. StatusBadge tones use color-mix (theme-adaptive); Button uses onPrimary; all #fff surfaces → surface.card across app + UI. Default stays light. Raw colorLight added for the A/B preview.
