---
"@xentral/web": minor
---

Go-live step 1: DB connection plumbing. Added pg + a server-only live-data boot that registers the Postgres-backed DataSource (createLiveDataSource) against the existing app schema. DORMANT by default — only activates when XENTRAL_LIVE_DATA=1 + DATABASE_URL are set (private host). Public preview keeps the flag unset → seed only, no PII. Verified the adapter reads the real DB (counts: contacts 26, accounts 40, leads 2, users 4, tenants 2).
