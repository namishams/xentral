---
"@xentral/web": minor
---

Admin surfaces — command-center design. Roles: KPI band (roles, members, avg permissions, scopes, largest role, read-only), scope chips and a permissions meter per role. Reseller: standardised KPI grid (monthly commission, active, in trial, conversion, tier, lifetime) + status chips. Admin Portal and Partner already carried command-center layouts. Seeded/DataPort (live behind auth); locked UI + tokens only.
