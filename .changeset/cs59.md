---
"@xentral/web": minor
---

ERP & Books lists — command-center design. Suppliers: KPI band (suppliers, open orders, active, categories, countries, idle) + category chips. Payables: KPI band (bills, to pay, overdue, paid, avg, suppliers) + status chips + per-group totals. Inventory: KPI band (SKUs, on hand, reserved, available, out of stock, warehouses) + warehouse chips, available = on hand − reserved. Payments: KPI band (received, pending, failed, avg, methods) + status chips + per-group totals. Seeded contracts (live behind auth); locked UI + tokens only.
