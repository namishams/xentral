---
"@xentral/web": minor
---

Customer Portal + public token pages (parity batch 6): /portal (magic-link customer view — outstanding invoices, quotes, documents), /pay/[token] (public secure payment), /q/[token] (public quote acceptance). Standalone branded layout, no app shell. tokens-only, theme-aware.
