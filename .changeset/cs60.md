---
"@xentral/web": minor
---

Orders & Warehouses — command-center design. Orders: KPI band (orders, in-progress value, fulfilled, open, avg, customers), status chips, per-group totals. Warehouses: KPI band (warehouses, items stored, avg capacity, near full, healthy, cities) and a capacity meter bar per location. Seeded contracts (live behind auth); locked UI + tokens only. This completes the ERP list surface.
