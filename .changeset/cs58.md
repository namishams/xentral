---
"@xentral/web": minor
---

Books & ERP lists: port the live app's command-center design. Invoices gets a KPI band (invoices, outstanding, collected, drafts, avg, customers), status filter chips and per-group totals. Quotes gets a KPI band (open value, accepted, win rate, avg) and status chips. Products (Items) gets a KPI band (items, in stock, needs attention, services, stock value, categories) and stock-health chips. Seeded contracts (live behind auth at go-live); locked UI + tokens only.
