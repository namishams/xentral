---
"@xentral/web": minor
---

WhatsApp (Meta Cloud API) live base — dormant. /api/whatsapp/webhook: GET verification handshake (verify_token against connected accounts or WHATSAPP_VERIFY_TOKEN) and POST that verifies the X-Hub-Signature-256 HMAC (WHATSAPP_APP_SECRET) then stores every inbound message (account -> conversation upsert -> message) so no inbound lead is lost at cutover. /api/whatsapp/send: outbound replies via graph.facebook.com using the account's token, tenant-checked, stores the message (internal notes skip Meta). Both DORMANT (503) until XENTRAL_LIVE_DATA=1 + DATABASE_URL (+ session for send). The AI auto-reply / intake agent is the next gradual step. Public preview unaffected.
