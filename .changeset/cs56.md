---
"@xentral/web": minor
---

Companies & Leads pages: port the live app's command-center design. Companies gets a KPI band, industry filter, owner chips, sort, and list/grid toggle with avatar logos and card grid. Leads gets a KPI band (qualified/working/hot/avg score), owner chips, and stage-grouped tables with avatar rows and a score meter. All computed from DataPort rows (seed on preview, live behind auth); locked UI + tokens only.
