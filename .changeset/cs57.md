---
"@xentral/web": minor
---

Activities, Tasks and Deals: port the live app's command-center design. Activities gets a KPI band, type filter chips and a vertical timeline with type glyphs. Tasks gets a KPI band (open/done/high/due-soon/overdue), priority+owner chips and checkable rows grouped Open/Done with due-date tones (completion persists in the browser). Deals gains a KPI band (open pipeline, won, win rate, avg, largest open) and per-group value subtotals on the existing interactive board. DataPort rows (seed on preview, live behind auth); locked UI + tokens only.
