---
"@xentral/config": minor
"@xentral/ui": minor
---

Deals list redesign (monday.com style): grouped sections (Active / Won / Lost) with a coloured
group accent + count, owner avatars, and pastel pipeline-stage pills. Added pastel `pipeline`
tokens to @xentral/config and a locked `StagePill` component to @xentral/ui. The grouped+pill
pattern is reusable for every list (Leads, Invoices, Orders…). View tabs (Main/Kanban/Forecast).
